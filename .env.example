PORT=8001
# Must be reachable from this Node process. Tokens must be issued by this same backend (same DB).
# If you see "Django API unreachable (got HTML/5xx)" or Cloudflare 521, the origin server is down or unreachable.
DJANGO_API_URL=https://system.evyatayatsewa.com
RECORDINGS_PATH=./recordings
CORS_ORIGIN=*
